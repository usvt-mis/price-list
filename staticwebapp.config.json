{
  "configurations": {
    "production": {
      "appLocation": "src",
      "apiLocation": "api",
      "outputLocation": "."
    }
  },
  "routes": [
    {
      "route": "/api/*",
      "methods": ["GET", "POST", "PUT", "DELETE", "OPTIONS"]
    }
  ]
}
